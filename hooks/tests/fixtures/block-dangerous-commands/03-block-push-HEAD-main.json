{
  "name": "block push refspec HEAD:main",
  "stdin": {
    "tool_input": {
      "command": "git push origin HEAD:main"
    }
  },
  "expect_exit": 0,
  "expect_stdout_contains": [
    "\"permissionDecision\":\"deny\""
  ]
}
