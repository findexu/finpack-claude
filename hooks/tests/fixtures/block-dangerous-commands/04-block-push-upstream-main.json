{
  "name": "block push to upstream main (not just origin)",
  "stdin": {
    "tool_input": {
      "command": "git push upstream main"
    }
  },
  "expect_exit": 0,
  "expect_stdout_contains": [
    "\"permissionDecision\":\"deny\""
  ]
}
