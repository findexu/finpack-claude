{
  "name": "block rm -rf $VAR (unknown-expansion conservative block)",
  "stdin": {
    "tool_input": {
      "command": "rm -rf $TRASH"
    }
  },
  "expect_exit": 0,
  "expect_stdout_contains": [
    "\"permissionDecision\":\"deny\""
  ]
}
