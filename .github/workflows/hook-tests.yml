name: hook-tests

on:
  pull_request:
    paths:
      - 'hooks/**'
      - 'scripts/**'
      - 'skills/**'
      - 'agents/**'
      - 'plugins/**'
      - '.claude-plugin/**'
      - '.github/workflows/hook-tests.yml'
  push:
    branches: [main]
    paths:
      - 'hooks/**'
      - 'scripts/**'
      - 'skills/**'
      - 'agents/**'
      - 'plugins/**'
      - '.claude-plugin/**'

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Install jq (macOS)
        if: runner.os == 'macOS'
        run: brew install jq || true
      - name: Install jq (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update -y && sudo apt-get install -y jq
      - name: Sync drift gate (plugins/ mirrors match sources)
        run: |
          bash scripts/sync-plugins.sh
          git add -N plugins/
          git diff --exit-code plugins/
      - name: Run hook fixtures
        run: bash hooks/tests/run-all.sh
